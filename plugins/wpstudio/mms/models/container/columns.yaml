columns:
    code:
        label: code
        type: text
        searchable: true
    name:
        label: name
        type: text
        searchable: true
    server:
        relation: server
        select: code
    destinationRole:
        relation: destinationRole
        select: name
    status:
        label: status
        type: text
    networkType:
        relation: networkType
        select: name
    created_at:
        label: created_at
        type: datetime
    updated_at:
        label: updated_at
        type: datetime
